# Copiez ce fichier en ".env" (jamais commite) et remplacez les valeurs
# avant tout deploiement reellement accessible depuis internet. En local,
# les valeurs par defaut de dev suffisent.
POSTGRES_PASSWORD=change_me
